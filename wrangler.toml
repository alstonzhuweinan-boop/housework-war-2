# Cloudflare Pages Configuration
# This file configures the build settings for Cloudflare Pages

name = "housework-war"
compatibility_date = "2025-03-13"

# Build configuration
[build]
command = "npm run build"

# Site configuration - points to the built files
[site]
bucket = "dist"
